<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head th:replace="fragments :: page_head('Categories - Shopme Admin')"></head>

<body>
	<div class="container-fluid">
		<div th:replace="navigation :: menu"></div>
		<div>
			<h2> Manage Categories</h2>
			<a th:href="@{/categories/new}" class="fa-solid fa-user-plus fa-2x icon-dark mr-3"></a>
			<a th:href="@{/categories/export/csv}" class="fa-solid fa-file-csv fa-2x icon-dark mr-3"></a>
		</div>
		<div th:if="${message != null}" class="alert alert-success text-center">
			[[${message}]]
		</div>
		
		<div th:replace="fragments :: search_form('/categories/page/1')"></div>
		
		<div class="full-details">
			<table class="table table-borded table-striped table-hover table-responsive-xl">
				<thead class="thead-dark">
					<tr>
						<th>ID</th>
						<th>Category Image</th>
						<th th:replace="fragments :: column_link('/categories', 'name', 'Category Name', 'none')"></th>
						<th class="hideable-column">Alias</th>
						<th>Enabled</th>
						<th>Action</th>
					</tr>
				</thead>
				<tbody>
					<tr th:each="cat : ${listCategories}">
						<td>[[${cat.id}]]</td>
						<td>
							<img th:src="@{${cat.imagePath}}" style="width:100px" class="image-fluid" />
						</td>
						<td>[[${cat.name}]]</td>
						<td class="hideable-column">[[${cat.alias}]]</td>
						<td>
					        <div th:replace="fragments :: status(${cat.enabled}, 'category', '/categories/' + ${cat.id})"></div>
					    </td>
					    <td>
					        <div th:replace="fragments :: edit('category', '/categories/edit/' + $cat.id})"></div>
					        &nbsp;&nbsp;
							<div th:replace="fragments :: delete('category', ${cat.id}, '/categories/delete/' + ${cat.id}, true)"></div>
					    </td>
					</tr>
				</tbody>
			</table>
		</div>
		
		<div class="less-details">
			<div th:each="cat : ${listCategories}" class="row m-1">
				<div class="col-4">
					<img th:src="@{${cat.imagePath}}" style="width: 100px" class="img-fluid" />
				</div>
				<div class="col-8">
					<div>[[${cat.name}]]</div>
					<div class="mt-2">
						<div th:replace="fragments :: status(${cat.enabled}, 'category', '/categories/' + ${cat.id})"></div>
						&nbsp;
						<div th:replace="fragments :: edit('category', '/categories/edit/' + $cat.id})"></div>
						&nbsp;&nbsp;
						<div th:replace="fragments :: delete('category', ${cat.id}, '/categories/delete/' + ${cat.id}, true)"></div>
					</div>
				</div>
			</div>	
		</div>
		
		<div class="text-center m-1" th:if="${totalItems} > 0">
			<span>Showing category # [[${startCount}]] to [[${endCount}]] of [[${totalItems}]]</span>
		</div>
		<div class="text-center m-1" th:unless="${totalItems} > 0">
			<span>No category found</span>
		</div>
		
		<div th:if="${totalPages > 1}">
			<nav>
			  <ul class="pagination justify-content-center flex-wrap">
			    <li th:class="${currentPage > 1 ? 'page-item' : 'page-item disabled'}">
			    	<a th:replace="fragments :: page-linkCat(1, 'First')"></a>
			    </li>
			    <li th:class="${currentPage > 1 ? 'page-item' : 'page-item disabled'}">
			    	<a th:replace="fragments :: page-linkCat(${currentPage - 1}, 'Previous')"></a>
			    </li>
			    
			    <li th:class="${currentPage != i ? 'page-item' : 'page-item active'}" th:each="i : ${#numbers.sequence(1, totalPages)}">
			    	<a th:replace="fragments :: page-linkCat(${i}, ${i})"></a>
			    </li>
			    
			    <li th:class="${currentPage < totalPages ? 'page-item' : 'page-item disabled'}">
			    	<a th:replace="fragments :: page-linkCat(${currentPage + 1}, 'Next')"></a>
			    </li>
			    <li th:class="${currentPage < totalPages ? 'page-item' : 'page-item disabled'}">
			    	<a th:replace="fragments :: page-linkCat(${totalPages}, 'Last')"></a>
			    </li>
			  </ul>
			</nav>
		</div>
		
		<div th:if="${keyword != null}" class="text-center">
			<span>Search result does not show  categories in hierarchical form</span>
		</div>
		
		<div th:replace="modal_fragments :: confirm_modal"></div>
		
		<div th:replace="fragments :: footer"></div>
	</div>
	<script th:src="@{/js/common_list.js}"></script>
	<script type="text/javascript">
		moduleURL = "[[@{/categories}]]";
		
		$(document).ready(function() {
			$(".link-delete").on("click", function(e) {
				e.preventDefault();
				showDeleteConfirmModal($(this), 'category');
				
			});
		});
	</script>
</body>
</html>